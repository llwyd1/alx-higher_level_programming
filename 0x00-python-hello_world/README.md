***This directory contains files for python-hello_world project***

File 0-run - is a shell script that runs a python script saved in the environment variable $PYFILE <br>

File 1-run_inline - is a shell script that runs Python code saved in environment variable $PYCODE <br>
